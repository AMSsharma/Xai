services:
  - type: web
    name: clinical-ai-decision-platform
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/main.py
    envVars:
      - key: PORT
        value: 8000
